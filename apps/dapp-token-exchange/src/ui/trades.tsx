import { Card, CardContent, CardTitle } from "@npcs/ui";
import React from "react";

interface ITradesProps {}

export const Trades: React.FC<ITradesProps> = (props) => {
  return (
    <Card>
      <CardTitle>Trades</CardTitle>
      <CardContent>Trades</CardContent>
    </Card>
  );
};
