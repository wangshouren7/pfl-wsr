import { Card, CardContent, CardTitle } from "@npcs/ui";
import React from "react";

interface IPriceChartProps {}

export const PriceChart: React.FC<IPriceChartProps> = (props) => {
  return (
    <Card>
      <CardTitle>Price Chart</CardTitle>
      <CardContent>Price Chart</CardContent>
    </Card>
  );
};
